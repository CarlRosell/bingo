{
  "name": "bingo",
  "description": "Simple app that will be used in my  fiance's preschool class",
  "version": "0.0.64",
  "dependencies": {
    "react": "16.0.0",
    "react-dom": "16.0.0",
    "serve": "^6.4.8"
  },
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp build/index.html build/404.html",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "now-start": "serve -s build/"
  }
}
